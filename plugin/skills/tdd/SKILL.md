---
name: tdd
description: 测试驱动开发，红-绿-重构循环。用户说"TDD"、"先写测试"、"红绿重构"、"测试先行"时触发。
---

# 测试驱动开发

## 核心理念

**测试应通过公开接口验证行为**，而不是验证实现细节。代码可以完全重写，测试不应该变。

**好测试**是集成风格的：通过公开 API 执行真实代码路径。描述系统**做**什么，而不是**怎么**做。

**坏测试**耦合在实现上：mock 内部协作、测私有方法、通过外部手段验证。标志：重构时测试挂了但行为没变。

## 反模式：水平切片

**不要先写所有测试再写所有实现。** 这样会测到"想象中的"行为，不是"真实的"行为。

**正确做法**：垂直切片。一个测试 → 一段实现 → 重复。

```
错误（水平）：
  RED:   test1, test2, test3, test4, test5
  GREEN: impl1, impl2, impl3, impl4, impl5

正确（垂直）：
  RED→GREEN: test1→impl1
  RED→GREEN: test2→impl2
  ...
```

## 工作流

### 1. 规划
写代码前确认：
- [ ] 接口变更
- [ ] 哪些行为优先测试
- [ ] 测试行为清单（不是实现步骤）

### 2. 示踪弹
写一个测试确认一件事：
```
RED:   写首个测试 → 失败
GREEN: 写最少代码让它通过
```

### 3. 增量循环
每个剩余行为重复：
```
RED:   写下个测试 → 失败
GREEN: 最少代码 → 通过
```

规则：
- 一次一个测试
- 只写够当前测试通过的代码
- 不要预判后面的测试
- 测试集中在可观察的行为

### 4. 重构
全部通过后：
- [ ] 提取重复
- [ ] 深化模块（简单接口背后藏复杂度）
- [ ] 自然运用 SOLID
- [ ] 每步重构后跑测试

**红色阶段不要重构。** 先到绿色。

## 每次循环检查清单

```
[ ] 测试描述行为，不是实现
[ ] 测试只用公开接口
[ ] 重构后测试依然有效
[ ] 代码只够当前测试通过
[ ] 没有投机性功能
```
