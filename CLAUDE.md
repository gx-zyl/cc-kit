
**权衡：**这些准则倾向于谨慎而非速度。对于简单的任务，请自行判断。

## 1. 编码前思考

**不要想当然。不要掩盖困惑。坦诚权衡。**

实施前：

- 明确陈述你的假设。如有疑问，请提出。
- 如果存在多种解释，请列出它们——不要默默选择。
- 如果存在更简单的方法，请说明。必要时提出异议。
- 如果有任何不清楚的地方，请停止。指出困惑之处。提出问题。

## 2. 简洁至上

**解决问题所需的最小代码量。不包含任何推测性代码。**

- 不添加超出要求的功能。
- 不添加用于一次性使用的代码的抽象。
- 不添加未经请求的“灵活性”或“可配置性”。
- 不处理不可能出现的情况的错误。
- 如果你写了 200 行代码，而它其实可以简化成 50 行，那就重写。

问问自己：“一位资深工程师会觉得这段代码过于复杂吗？” 如果会，那就简化它。

## 3. 精准修改

**只修改必要的部分。只清理你自己造成的烂摊子。**

编辑现有代码时：

- 不要“改进”相邻的代码、注释或格式。
- 不要重构没有问题的代码。
- 保持与现有代码风格一致，即使你的想法不同。
- 如果你发现无关的无用代码，请指出——不要删除它。

当你的修改导致孤立代码出现时：

- 删除你修改后不再使用的导入/变量/函数。
- 除非被要求，否则不要删除已有的无用代码。

测试：每一行修改后的代码都应该直接对应用户的请求。

## 4. 目标驱动的执行

**定义成功标准。**循环直至验证通过。**

将任务转化为可验证的目标：

- “添加验证” → “编写无效输入的测试，并确保测试通过”
- “修复错误” → “编写可复现该错误的测试，并确保测试通过”
- “重构 X” → “确保重构前后的测试均通过”

对于多步骤任务，请简要说明计划：

```yaml

1. [步骤] → 验证：[检查]
2. [步骤] → 验证：[检查]
3. [步骤] → 验证：[检查]

```

严格的成功标准允许您独立循环执行。而较弱的标准（例如“使其正常工作”）则需要不断澄清。

---

**如果以下情况发生，则这些准则有效：**差异中不必要的更改较少，因过度复杂化而导致的重写较少，并且澄清问题发生在实现之前而不是错误之后。



## grill-me

1. 回答结束后：如果有重大发现或者遗漏让用户确认是否计入文档，同时反问~3个用户未意识到的问题。


## 文档维护

任务开始或结束按需维护工作目录 `docs/` 下的文档：

| 文件 | 用途 | 约束 |
|------|------|------|
| STATE.md | 当前进展 | ≤300 行 |

## 文档风格

主图 Mermaid（设计），辅图 ASCII（链路矩阵流程图），编排流YAML（工作流）。

## 优先组合现有代码

## 插件探索 /insights /powerup

## 任务完成后，必须给出证据。风格：链路矩阵流程 + 消费方 + 卡通 + 解读步骤。

- 最后总结解决问题的方法、步骤输出到 ./fix1/.docs/plan-impl/<年-月-日>/[模块]-[功能]-[问题].md 

## 如果做迁移、重构等任务要求：不要压缩、保证完整、有效，合并零散的同类内容。